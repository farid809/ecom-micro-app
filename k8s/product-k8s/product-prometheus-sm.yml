apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: product-app
  namespace: ecom3
  labels:
    team: ecom3
spec:
  selector:
    matchLabels:
      app: product
  endpoints:
    - port: web
      path: /prometheusMetrics
