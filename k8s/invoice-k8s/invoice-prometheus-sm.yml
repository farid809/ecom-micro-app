apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: invoice-app
  namespace: ecom3
  labels:
    team: ecom3
spec:
  selector:
    matchLabels:
      app: invoice
  endpoints:
    - port: web
      path: /prometheusMetrics
