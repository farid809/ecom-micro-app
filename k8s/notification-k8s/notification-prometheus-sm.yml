apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: notification-app
  namespace: ecom3
  labels:
    team: ecom3
spec:
  selector:
    matchLabels:
      app: notification
  endpoints:
    - port: web
      path: /prometheusMetrics
